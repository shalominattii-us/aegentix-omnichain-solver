{
  "name": "aegentix-omnichain-solver",
  "version": "1.0.0",
  "private": true,
  "description": "Aegentix Multi-Strategy Orchestrator Engine - inventory-based omnichain intent solver + spatial arb",
  "main": "orchestrator.js",
  "scripts": {
    "start": "node orchestrator.js",
    "dev": "node --watch orchestrator.js",
    "test": "node --test tests/*.test.js",
    "lint": "echo 'no linter configured'"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "ioredis": "^5.4.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
